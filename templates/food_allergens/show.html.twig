{% extends 'base.html.twig' %}

{% block title %}FoodAllergens{% endblock %}

{% block body %}
<div class="container-2">
    <h1>FoodAllergens</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ food_allergen.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ food_allergen.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('food_allergens_index') }}">back to list</a>

    <a href="{{ path('food_allergens_edit', {'id': food_allergen.id}) }}">edit</a>

    {{ include('food_allergens/_delete_form.html.twig') }}
</div>
{% endblock %}
