{% extends 'base.html.twig' %}

{% block title %}RestaurantCategory{% endblock %}

{% block body %}
    <h1>RestaurantCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ restaurant_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ restaurant_category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('restaurant_category_index') }}">back to list</a>

    <a href="{{ path('restaurant_category_edit', {'id': restaurant_category.id}) }}">edit</a>

    {{ include('restaurant_category/_delete_form.html.twig') }}
{% endblock %}
