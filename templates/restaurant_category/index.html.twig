{% extends 'base.html.twig' %}

{% block title %}RestaurantCategory index{% endblock %}

{% block body %}
    <h1>RestaurantCategory index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for restaurant_category in restaurant_categories %}
            <tr>
                <td>{{ restaurant_category.id }}</td>
                <td>{{ restaurant_category.name }}</td>
                <td>
                    <a href="{{ path('restaurant_category_show', {'id': restaurant_category.id}) }}">show</a>
                    <a href="{{ path('restaurant_category_edit', {'id': restaurant_category.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('restaurant_category_new') }}">Create new</a>
{% endblock %}
